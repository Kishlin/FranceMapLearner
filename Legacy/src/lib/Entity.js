export default class Entity {
    constructor(id, name, nameSlug, hometown, hometownSlug) {
        this.id = id;
        this.name = name;
        this.nameSlug = nameSlug;
        this.hometown = hometown;
        this.hometownSlug = hometownSlug;
    }
}
